# These are template files for node configuration.
#
# The format is <name> = <value>
#
# Comment lines start with #
# Blank lines are ignored
# Blank spaces are ignored before the value, but are allowed in the value itself, e.g. see FILE_FORMAT_VERSION below
# Values with multiple instances are comma separated
#

